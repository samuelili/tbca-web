nav.navbar.navbar-custom.navbar-fixed-top.at-top
  .container-fluid
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar-collapse', aria-expanded='false')
        span.icon-bar
        span.icon-bar
        span.icon-bar

      a.navbar-brand(href='/')
        img(alt='tbca', src='/images/tbca-white.png')
    .collapse.navbar-collapse.text-right#navbar-collapse
      ul.nav.navbar-nav.pull-right
        li(class=item == 1 ? 'active' : '')
          a.text-right(href="/job.html") Job
        li(class=item == 2 ? 'active' : '')
          a.text-right(href="/about.html") About
        li(class=item == 3 ? 'active' : '')
          a.text-right(href="/contact.html") Contact